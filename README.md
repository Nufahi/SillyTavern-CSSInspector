# SillyTavern-CSSInspector
 A SillyTavern extension that adds a browser-like CSS inspector to your chat UI. Hover or tap any element to see its classes, ID, dimensions, computed styles, CSS variables, and box model — with one-click copy. Supports desktop and mobile (touch drag &amp; tap-to-inspect). Fully themeable (dark/light).
